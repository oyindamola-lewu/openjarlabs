const ExperimentalModal = () => {
  return (
    <div className="experimental-modal">
      {/* TODO: Replace with modal structure (overlay, form, close button) */}
    </div>
  );
};

export default ExperimentalModal;
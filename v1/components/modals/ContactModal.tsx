

// ContactModal Component
// This modal will handle the contact form (name, email, message).
// TODO: Implement form fields, validation, and submission handling.

const ContactModal = () => {
  return (
    <div className="contact-modal">
      {/* TODO: Replace with modal structure (overlay, form, close button) */}
    </div>
  );
};

export default ContactModal;
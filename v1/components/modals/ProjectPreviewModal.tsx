const ProjectPreviewModal = () => {
  return (
    <div className="projectpreview-modal">
      {/* TODO: Replace with modal structure (overlay, form, close button) */}
    </div>
  );
};

export default ProjectPreviewModal;
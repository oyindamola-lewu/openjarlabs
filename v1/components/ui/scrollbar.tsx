// Scrollbar Component
// A custom scrollbar styled to match the liquid-glass aesthetic of the site.
// TODO: Implement styling and props for orientation, size, and thumb style.

const Scrollbar = () => {
  return (
    <div className="ui-scrollbar">
      {/* TODO: Replace with custom scrollbar logic and styling */}
    </div>
  );
};

export default Scrollbar;
